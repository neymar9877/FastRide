<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F5F5F5">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Incoming Ride Requests"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="#1F4E79"
        android:padding="16dp"
        android:background="#FFFFFF"/>

    <TextView
        android:id="@+id/tvSwipeHint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="✅ Swipe RIGHT to accept   |   ❌ Swipe LEFT to decline"
        android:textSize="13sp"
        android:textColor="#FFFFFF"
        android:background="#2E75B6"
        android:padding="10dp"
        android:gravity="center"
        android:visibility="gone"/>

    <TextView
        android:id="@+id/tvNoRequests"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="No ride requests at the moment.\nWaiting for passengers..."
        android:textSize="16sp"
        android:textColor="#888888"
        android:gravity="center"
        android:visibility="visible"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerRideRequests"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp"
        android:visibility="gone"/>

</LinearLayout>
